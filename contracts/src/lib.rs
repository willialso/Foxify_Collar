#![allow(dead_code)]

/// Placeholder contract interface for future on-chain hooks.
/// This repository keeps settlement and execution off-chain for speed.
pub struct CollarSettlement {}

impl CollarSettlement {
    pub fn new() -> Self {
        Self {}
    }
}
