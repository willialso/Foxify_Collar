# Widget Embed

The protection widget is a standalone React component intended for iframe or module embedding.

## Embed Options
- Iframe to `apps/web` build output
- Web component wrapper (future)

## Required Data
- drawdown buffer
- time remaining
- pnl
- protection cost + expiry
